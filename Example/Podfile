use_frameworks!
platform :ios, '10.0'

target 'Harmony_Example' do
  project 'Harmony.xcodeproj'
  
  pod 'Harmony', :path => '../'
  pod 'HarmonyAlamofire', :path => '../'
  
  pod 'Kingfisher', '~> 5.3'
  pod 'PromisesSwift', '~> 1.2'
  
  pod 'SQLite.swift'

  target 'Harmony_Tests' do
    inherit! :search_paths
  end
end
